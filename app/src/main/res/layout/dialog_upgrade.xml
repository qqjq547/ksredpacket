<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="260dp"
    android:layout_height="315dp"
    android:gravity="center_horizontal"
    android:background="@drawable/bg_upgrade">
   <ProgressBar
       android:id="@+id/pb_upgrade"
       style="@android:style/Widget.ProgressBar.Horizontal"
       android:layout_width="@dimen/space200"
       android:layout_height="@dimen/space8"
       android:indeterminate="false"
       android:max="100"
       android:layout_marginTop="@dimen/space230"
       android:progressDrawable="@drawable/bg_upgrade_selector" />
</LinearLayout>
